---
title: "`IttyBittyTable`: PDF documents"
include-headers:
  - \usepackage{codehigh}
  - \usepackage{verbatim}
---

```{r}
library(IttyBittyTable)
options(tt_latexOptions_placement = "H")
options(tt_output_default = "latex")
x <- mtcars[1:4, 1:5]
```

```{r}
IttyBittyTable(x) |> style_row(2, color = "blue", background = "pink")
```

```{r}
IttyBittyTable(x) |>
  style_col(1:2, color = "white", background = "black", bold = TRUE, italic = TRUE)
```

```{r}
IttyBittyTable(x) |> style_row(italic = TRUE, bold = TRUE, color = "blue", background = "pink")
```

```{r}
IttyBittyTable(x) |> style_col(background = "green", color = "white") |> cat(file = "~/Downloads/trash.html")
```

```{r}
IttyBittyTable(x) |>
  style_row(2, latex = latexOptions(cmd = "\\LARGE\\texttt")) |>
  style_col(2, latex = latexOptions(cmd = "\\footnotesize\\texttt"))
```

```{r}
IttyBittyTable(x) |> style_col(c(1, 4), italic = TRUE)
```

```{r}
IttyBittyTable(x) |> style_row(2:3, background = "olive", color = "white")
```

```{r}
IttyBittyTable(x) |> style_row(2:3,
  latex = latexOptions(
    cmd = "\\texttt"
    ))
```

```{r}
IttyBittyTable(x, latex = latexOptions(theme = "grid"))
```

```{r}
IttyBittyTable(x,
  latex = latexOptions(
    theme = "void",
    hlines = "dotted,fg=blue",
    vlines = "dashed,fg=red,wd=1pt")
)
```

```{r}
IttyBittyTable(x)
```

# Themes

`IttyBittyTable` includes three basic "themes" for LaTeX tables: `"booktabs"` (default), `"grid"`, and `"void"`. The theme can be selected using the `latexOptions()` function.

```{r}
IttyBittyTable(x, latex = latexOptions(theme = "booktabs"))
```

```{r}
IttyBittyTable(x, latex = latexOptions(theme = "grid"))
```

```{r}
IttyBittyTable(x, latex = latexOptions(theme = "void"))
```

The `"void"` theme can be useful when combined with the `hline*`  and `vline*` arguments. For example:

```{r}
IttyBittyTable(x,
  latex = latexOptions(
    theme = "void",
    hline = c("1,6", "fg=orange,wd=2pt"),
    hline = c("2", "fg=orange,wd=1pt"),
    vlines = "dotted,fg=olive,wd=1pt",
  )
)
```


## Verbatim (escape)


```{r}
library(IttyBittyTable)
options(tt_latexOptions_placement = "H")
options(tt_output_default = "latex")
k <- data.frame(
  verbatim = c("a_b", "$a_b$"),
  evaluated = c("a\\_b", "$a_b$")
)
IttyBittyTable(k) |> 
  style_col(1, latex = latexOptions(cmd = "\\fakeverb")) |> cat()
```


