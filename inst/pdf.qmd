---
title: "`tinytable`: PDF documents"
---

```{r}
library(tinytable)
options(tt_latexOptions_placement = "H")
options(tt_output_default = "latex")
x <- mtcars[1:4, 1:5]
```

```{r}
tinytable(x) |> style_row(2, color = "blue", background = "pink")
```

```{r}
tinytable(x) |>
  style_column(1:2, color = "white", background = "black", bold = TRUE, italic = TRUE)
```

```{r}
tinytable(x) |> style_row(italic = TRUE, bold = TRUE, color = "blue", background = "pink")
```

```{r}
tinytable(x) |> style_column(background = "green", color = "white") |> cat(file = "~/Downloads/trash.html")
```

```{r}
tinytable(x) |>
  style_row(2, latex = latexOptions(cmd = "\\LARGE\\texttt")) |>
  style_column(2, latex = latexOptions(cmd = "\\footnotesize\\texttt"))
```

```{r}
tinytable(x) |> style_column(c(1, 4), italic = TRUE)
```

```{r}
tinytable(x) |> style_row(2:3, background = "olive", color = "white")
```

```{r}
tinytable(x) |> style_row(2:3,
  latex = latexOptions(
    cmd = "\\texttt"
    ))
```

```{r}
tinytable(x, latex = latexOptions(theme = "grid"))
```

```{r}
tinytable(x,
  latex = latexOptions(
    theme = "void",
    hlines = "dotted,fg=blue",
    vlines = "dashed,fg=red,wd=1pt")
)
```

```{r}
tinytable(x)
```

# Themes

`tinytable` includes three basic "themes" for LaTeX tables: `"booktabs"` (default), `"grid"`, and `"void"`. The theme can be selected using the `latexOptions()` function.

```{r}
tinytable(x, latex = latexOptions(theme = "booktabs"))
```

```{r}
tinytable(x, latex = latexOptions(theme = "grid"))
```

```{r}
tinytable(x, latex = latexOptions(theme = "void"))
```

The `"void"` theme can be useful when combined with the `hline*`  and `vline*` arguments. For example:

```{r}
tinytable(x,
  latex = latexOptions(
    theme = "void",
    hline = c("1,6", "fg=orange,wd=2pt"),
    hline = c("2", "fg=orange,wd=1pt"),
    vlines = "dotted,fg=olive,wd=1pt",
  )
)
```


